# Paths
DATASET_PATH: datasets/dataset.csv
CLEANED_DATASET_PATH: datasets/cleaned_dataset.csv

# Shared defaults across the project live here.
# Embedding-specific settings live in embedding_config.yaml.
# Classical ML settings live in ml_config.yaml.
# Deep learning settings live in dl_config.yaml.

